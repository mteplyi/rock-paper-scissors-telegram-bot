const ROCK_CODE = 'r';
const PAPER_CODE = 'p';
const SCISSORS_CODE = 's';
const STOP_CODE = 'x';
const HAND_CODES = Object.freeze([
  ROCK_CODE,
  PAPER_CODE,
  SCISSORS_CODE,
]);

module.exports = Object.freeze({
  ROCK_CODE,
  PAPER_CODE,
  SCISSORS_CODE,
  STOP_CODE,
  HAND_CODES,
});
